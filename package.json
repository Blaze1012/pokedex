{
  "dependencies": {
    "axios": "^1.3.5",
    "mongoose": "^7.0.3"
  },
  "name": "react-new1",
  "version": "1.0.0",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server":"npm run server --prefix backend",
    "client":"npm run dev --prefix vite-project",
    "build":"npm run build --prefix vite-project",
    "start":"npm run server",
    "install_master": "npm install & npm run install_client & npm run install_server",
    "install_client":"npm install --prefix vite-project",
    "install_server":"npm install --prefix backend"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
